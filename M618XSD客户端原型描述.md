# DELUX M618XSD 客户端 · 原型描述文档

> **文档用途**：参考 ATK HUB（`https://v3-hub.atkgear.com/`）驱动 Web 端原型，为 DELUX M618XSD 鼠标客户端（WPF 桌面应用）设计**自有原型规格**。
> **导航页名称**：沿用参考导航（改键设置 / 快捷指令 / DPI设置 / 性能设置 / 参数设置 / 配置管理 / 其他设置），各页承载本产品对应功能：
> | 导航页名称 | 页面承载功能 |
> |---|---|
> | 改键设置 | 按键映射（10 键重映射） |
> | 快捷指令 | 宏管理（录制/编辑/绑定宏） |
> | DPI设置 | DPI 挡位 |
> | 性能设置 | 回报率 + 按键响应时间（去抖） |
> | 参数设置 | 灯光与电源 |
> | 配置管理 | 配置管理（未实现，原型先行） |
> | 其他设置 | 电量 / 产品外观 / 恢复出厂 / 官网驱动下载 / 关于 |
>
> **工作流**：参考文档仅详细描述了「改键设置」「快捷指令」两页；其余页面依据参考软件的**侧边导航 + 顶部状态条 + 主内容区**整体布局自行设计。每页遵循统一骨架：**标题栏（页名 + 一句话说明）→ 配置区（卡片化）→ 自动保存（停止操作约 1.5 秒后自动写入）**。
> **平台差异**：参考为 Web 网页（1366×768）；本产品为 WPF 桌面窗口（最小 900×640，可缩放），导航与状态条形态做对应改造。
> **文案底线**：全站面向普通用户，不出现「0x08」「校验和」「Feature Report」「HID」等协议术语；错误提示只给「怎么办」；原始字节区默认折叠、仅进阶用户可展开。

---

## 〇、全局设计规范

| 项 | 参考值（ATK） | 本产品取值 | 备注 |
|----|--------------|-----------|------|
| 整体风格 | 浅灰白背景，极简、垂直居中 | 浅灰白背景，Fluent 极简、卡片化（**纯原生 WPF**，自研主题字典 + Segoe MDL2 图标） | 桌面应用 |
| 主色 | 橙色（Logo/强调态）、黑色（文字/主按钮） | 橙色（Logo/选中态/强调）、黑色（文字/主按钮） | 一致 |
| 辅助色 | 浅灰描边（次按钮）、灰字（说明） | 同左 + 红（危险/断连/警告）、绿（成功/已连接） | 增加状态色 |
| 圆角 | 按钮中等圆角 | WPF-UI 默认中等圆角 | 全站统一 |
| 顶部栏 | 固定顶部，左 Logo、右操作区 | 固定顶部，左 Logo+产品名、右状态区 | 见 0.1 |
| 侧边栏 | 设备信息→配置切换→功能菜单→广告/反馈 | 设备信息→配置切换→功能菜单→管理→帮助 | 见 0.2 |
| 主区域 | 单列垂直居中 | 标题栏→配置卡片→自动保存（防抖 1.5s） | 无操作区按钮 |
| 主题 | 明暗切换 | 浅色/深色跟随系统，可手动切换（WPF-UI 内置） | 一致 |

### 0.1 顶部栏（所有页统一）

```
[品牌Logo + 产品名]          [连接状态条] [官方驱动警告条] [断开连接] [🌙] [⚙]
```

| 元素 | 视觉 | 文案/行为 |
|------|------|----------|
| 品牌 Logo | 橙色 Logo + 黑色「DELUX M618XSD」 | 替换 ATK GEAR 为自研 Logo |
| 连接状态条 | 圆点 + 文案 + 「连接/断开」按钮 | 绿=已连接 / 灰=未连接 / 黄=连接中 |
| 官方驱动警告条 | 黄色药丸（条件显示） | 「检测到官方驱动运行中，为避免冲突请先关闭」+「打开任务管理器」；出现时拦截一切发送 |
| 断开连接 | 红色实心按钮（连接时显示） | 点击断开当前鼠标 |
| 主题切换 | 月亮/太阳图标 | 浅/深色切换 |
| 设置 | 齿轮图标 | 跳转「其他设置」页 |

### 0.2 左侧边栏（所有页统一）

```
┌─────────────────┐
│ ⚡ 86%   M618XSD │   ← 设备信息：电量 + 设备名
│ 当前配置：日常 ▼  │   ← 配置切换：当前 Profile 下拉
├─────────────────┤
│ ▣ 改键设置       │   ← 功能菜单（鼠标配置）
│ ☰ 快捷指令       │
│ ⇱ DPI设置        │
│ ◉ 性能设置       │
│ ✦ 参数设置       │
├─────────────────┤
│ ⚙ 配置管理       │   ← 管理
│ ☰ 其他设置       │
├─────────────────┤
│ ? 帮助与反馈     │   ← 帮助
└─────────────────┘
```

> 未连接时：设备信息区显示「—」并置灰，全部功能菜单项不可点，主区域显示统一引导空态。

### 0.3 反馈与防呆（全站）

- **自动保存**：修改后停止操作约 1.5 秒自动写入设备（防抖合并），卡片右上角出现「待保存」角标，保存中显示「正在保存…」，成功后角标消失。
- **成功 Toast**：保存成功后右下角轻提示（如「已保存，鼠标灯光已切换」），自动消失。
- **危险操作**（按键整表覆写、电源命令、恢复出厂、删除 Profile、覆盖 Profile）一律二次确认。
- **恢复默认**：仅在「其他设置」提供「恢复出厂设置」，一键恢复全部配置为出厂默认值。
- **断连保护**：断连时禁止写入、顶部状态条变灰，**已修改未保存的内容保留**，重连后自动保存。

---

## 一、页面：连接设备等待页（Connect / Waiting）

- **触发方式**：启动时自动尝试连接；未连接成功时停在本页。
- **当前状态**：未连接任何设备，展示等待引导。

### 1.1 布局结构

```
┌──────────────────────────────────────────────┐
│                                              │
│        ┌────────────────────────────┐         │
│        │    [ M618XSD 鼠标插画 ]     │         │  ← 卡片背景+圆角边框，MaxHeight=420，等比缩小不放大
│        └────────────────────────────┘         │
│                                              │
│           [ 正在识别鼠标 ]                     │  ← 大标题，连接成功后变「鼠标已连接」
│      [ 驱动无法识别蓝牙模式连接，               │  ← 连接成功后变引导文案
│        请使用 2.4G 接收器或数据线 ]            │
│           [===== 进度环 =====]                 │  ← 连接中显示，不确定进度条
│      ┌──────────────────────────────┐         │
│      │ 连接失败原因（红底白字）        │         │  ← 失败时显示
│      │ [打开任务管理器] [重新识别]    │         │
│      └──────────────────────────────┘         │
└──────────────────────────────────────────────┘
```

### 1.2 主区域组件

| 元素 | 视觉样式 | 文案 | 说明 |
|------|----------|------|------|
| 设备大图 | 卡片背景+圆角边框，`MaxHeight=420`，等比缩小不放大 | — | 外观跟随「其他设置→产品外观」切换黑/白/蓝 |
| 主标题 | 大号黑色文字，居中 | 未连接时「正在识别鼠标」；已连接时「鼠标已连接」 | 连接成功后自动跳转改键页，不会长期显示「鼠标已连接」 |
| 副说明 | 灰色小字，居中，自动换行 | 未连接时「驱动无法识别蓝牙模式连接，请使用 2.4G 接收器或数据线」；已连接时「已识别到 DELUX M618XSD，可从左侧进入各项设置。」 | — |
| 连接进度环 | 不确定进度条，宽120，居中 | — | `IsBusy` 时显示，连接中或自动识别中 |
| 错误提示区 | 红色背景(`#FDECEA`)圆角卡片，红字(`#B00020`) | 按场景区分（见 1.3） | 内含「打开任务管理器」和「重新识别」按钮，按场景分别显示 |

### 1.3 连接逻辑

- **自动连接**：软件启动后即自动尝试识别鼠标（`AutoConnect()`），无需用户点击。
- 蓝牙模式不被驱动识别，必须通过 **2.4G 接收器**或**数据线**连接。
- 连接成功 → 自动进入「设备配置页 - 改键设置」。
- **连接超时**：8 秒内未收到鼠标 Input 上报则停止，显示「重新识别」按钮。
- 连接失败提示区分场景，给可执行文案：
  - 未插接收器 → 「未找到鼠标：请插入 2.4G 接收器或数据线后重试。」
  - 官方驱动运行 → 顶部黄色警告条「检测到官方驱动 Mouse.exe 正在运行，请先完全退出后再使用本软件，否则可能无法写入配置。」+ 错误区「打开任务管理器」按钮
  - 设备被占用 → 「鼠标正被其他程序占用，请关闭官方驱动后重试。」
  - 接收器已检测到但鼠标未开机 → 「已检测到接收器，但鼠标未开机或无响应，请打开鼠标后点击重新识别。」
  - 其他 → 「连接失败，请确认接收器已插入、官方驱动已关闭后重试。」

### 1.4 交互说明

- 本产品通过 2.4G/数据线自动识别设备，**不提供手动选择鼠标型号弹窗**。
- 无「新增设备」按钮：启动即自动扫描，超时或失败后通过「重新识别」按钮手动重试。
- 连接成功后页面自动切换到设备配置页，当前页不作停留。

---

## 二、页面：设备配置页 - 改键设置（Key Remapping）

- **触发方式**：设备连接成功后自动进入，首屏默认展示本页。
- **当前状态**：已连接，展示鼠标俯视示意图 + 10 个可编程按钮标签。

### 2.1 布局结构

```
┌──────────────────────────────────────────────────────────┐
│ [Logo 产品名]   [●已连接][断开连接][🌙][⚙]                │  ← 顶部栏
├────────┬─────────────────────────────────────────────────┤
│ 86%    │                                                 │
│ M618XSD│       [ M618XSD 垂直鼠标侧视图 ]                  │  ← 主内容区
│ 日常 ▼ │        左键 右键 滚轮 前进 后退                 │
│ ────── │        DPI键 左滚 右滚 上滚 下滚                │
│ 改键设置│                                                 │
│ 快捷指令│                  [ 待保存 ]                      │  ← 卡片右上角角标（防抖 1.5s 后自动写入）
│ DPI设置 │                                                 │
│ 性能设置│   [ 原始字节 ▾（默认折叠）]                     │
│ 参数设置│                                                 │
│ 配置管理│                                                 │
│ 其他设置│                                                 │
└────────┴─────────────────────────────────────────────────┘
```

### 2.2 主内容区 - 按键映射

- **设备可视化**：中央 M618XSD 垂直鼠标示意图（外观随「产品外观」设置切换黑/白）。
- **可编程按钮标签**（10 个，围绕鼠标图，白底标签 + 橙色虚线定位点）：

| 标签 | 默认功能 | 说明 |
|------|---------|------|
| 左键 | 左键点击 | 主键 |
| 右键 | 右键点击 | 主键 |
| 滚轮 | 中键点击 | 按下滚轮 |
| 前进 | 前进 | 侧面前进键 |
| 后退 | 后退 | 侧面后退键 |
| DPI键 | DPI循环 | DPI 切换键 |
| 左滚 | 向左滚动 | 拇指滚轮 ↑ |
| 右滚 | 向右滚动 | 拇指滚轮 ↓ |
| 上滚 | 向上滚动 | 滚轮 ↑ |
| 下滚 | 向下滚动 | 滚轮 ↓ |

### 2.3 侧边栏变化

与等待页相比，侧边栏变为「设备信息 + 配置切换 + 功能菜单 + 管理 + 帮助」，当前项「改键设置」高亮。

### 2.4 交互流程

```
点击任一按钮标签
  → （左键等关键键）风险弹窗「当前只有一个左键，改了之后可能无法点击」[取消/我知道了]
  → 右侧滑出「动作分配面板」（见页面三）
  → 选好功能后，面板底部出现 [取消][确定]
  → 回到主区，该按钮标签显示新功能名 + 卡片出现「待保存」角标
  → 停止操作约 1.5 秒 → 自动写入整张按键映射表 → Toast「已保存」
```

### 2.5 对产品参考意义

- 设备可视化 + 按钮标注 + 自动保存的布局可直接复用。
- 0x08 为整表覆写、不可读（协议事实）→ **UI 不提供「读取按键表」**，本地维护全表副本，改一项整表写出，自动保存前确认弹窗明示「未修改按键保持不变」。

---

## 三、页面/面板：改键设置 - 动作分配面板（Action Assignment）

- **触发方式**：改键设置页点击任一按钮标签。
- **形态**：右侧滑出抽屉式面板（约主区 1/3 宽），非跳转新页；被选中的按钮标签变为橙色实心高亮。

### 3.1 布局结构

```
┌────────┬───────────────────────────────┬───────────────────┐
│ 侧边栏 │                               │  系统 键盘 特殊 宏 │  ← Tab
│        │      [ 鼠标俯视图 ]            │  [🔍 搜索]         │
│        │ 左键(高亮) 右键 滚轮 前进 后退 │  ────────────────  │
│        │                               │  鼠标操作           │
│        │      [ 待保存 ]               │    禁用 左键 右键… │
│        │                               │  ────────────────  │
│        │                               │  DPI功能            │
│        │                               │    DPI循环          │
│        │                               │  ────────────────  │
│        │                               │  宏（已录制）       │
│        │                               │  ────────────────  │
│        │                               │  快捷键/多媒体/…    │
│        │                               │   （置灰·待补齐）    │
└────────┴───────────────────────────────┴───────────────────┘
```

### 3.2 顶部分类 Tab

| Tab | 状态 | 内容 |
|-----|------|------|
| 系统 | 默认选中（黑底白字） | 已确认功能的分组 |
| 键盘 | 置灰 | 直接映射键盘按键 → 待协议补齐 |
| 特殊 | 置灰 | 特殊功能 → 待协议补齐 |
| 宏 | 灰色未选中 | 用户已录制的宏列表（无宏时显示「去快捷指令页新建」入口） |

### 3.3 搜索框

- 位置：Tab 下方；样式：带放大镜图标输入框；占位文案：「搜索」。

### 3.4 动作分组（系统 Tab）

| 分组 | 动作项 | 状态 |
|------|--------|------|
| 鼠标操作 | 禁用 / 左键 / 右键 / 中键 / 前进 / 后退 / 上滚 / 下滚 / 左滚 / 右滚 | ✅ 已实机验证 |
| DPI功能 | DPI循环 | ✅ 已实机验证 |
| DPI功能 | DPI+ / DPI- / DPI固定值 | 置灰「等待协议补齐」 |
| 配置切换 | Profile循环 / Profile+ / Profile- | 置灰「等待协议补齐」 |
| 宏 | 已录制宏列表（选择后二级设置播放方式） | ✅ 已实机验证 |
| 快捷键 | 剪切/复制/粘贴/撤销/锁屏/截屏… | 置灰「等待协议补齐」 |
| 多媒体 | 播放/暂停/上一曲/下一曲/音量+/静音… | 置灰「等待协议补齐」 |
| 浏览器 | 主页/收藏夹/刷新/前进/后退… | 置灰「等待协议补齐」 |
| 系统 | 邮件/计算器/我的电脑… | 置灰「等待协议补齐」 |
| 其他 | 按键关闭（Button Off） | 置灰「等待协议补齐」 |

> **置灰原则**：未逆向的功能码**绝不进 UI 下拉**，置灰并标注「等待协议补齐」，避免用户选到不可用的功能（对应「宁可少一个功能，不可多一分困惑」）。

### 3.5 宏绑定（系统 Tab → 宏分组）

- 选中「宏」后，面板展开二级区：宏下拉 + 播放方式三选一（循环次数 / 任意键停止 / 按住循环）。
- 宏内容需在「快捷指令」页新建；无宏时给出「去快捷指令页新建」入口。

### 3.6 交互细节

- 关键按键（左键等）改动前先弹风险提示，避免用户把自己锁死。
- 原始字节区（进阶）：主区底部可折叠，显示该按钮当前 3 字节值（Consolas 等宽字体），默认折叠。

### 3.7 对产品参考意义

- 右侧抽屉 + Tab 分类 + 分组列表可直接复用。
- 已确认/未确认功能用「可用/置灰」明确区分，是普通用户不踩坑的关键。

---

## 四、页面：快捷指令（Macro）

> 对应参考「快捷指令」，承载宏管理功能。参考以「槽位 M1、M1_copy」管理；本产品按「宏列表（可命名）」管理，交互结构复用参考的**列表 + 右侧抽屉编辑器**。

- **触发方式**：左侧菜单 → 快捷指令。
- **当前状态**：无宏时展示空状态。

### 4.1 布局结构

```
┌────────┬─────────────────────────────────────────────────┐
│ 86%    │  [ 快捷指令 · 录制一串按键动作，绑定到鼠标按键 ]     │  ← 标题栏
│ M618XSD│                                                 │
│ 日常 ▼ │         [ 暂无宏配置 ]                            │  ← 空状态
│ ────── │            [ + 新建宏 ]                           │
│ 改键设置│                                                 │
│ 快捷指令│                                                 │  ← 当前选中
│ DPI设置 │                                                 │
│ 性能设置│                                                 │
│ 参数设置│                                                 │
│ 配置管理│                                                 │
│ 其他设置│                                                 │
└────────┴─────────────────────────────────────────────────┘
```

### 4.2 有宏时的布局

```
┌────────┬───────────────────────────┬───────────────────────────┐
│ 侧边栏 │  宏列表（左侧）           │  当前宏摘要（右侧）        │
│        │  [＋][－] [导入][导出]     │  · 宏名称 / 步骤数 / 播放方式 │
│        │  ├─ 宏1（选中高亮）       │  [ 编辑 ] [ 复制 ] [ 删除 ] │
│        │  ├─ 宏2                   │                           │
│        │  └─ ...                   │  [ 绑定按键提示 ]           │
└────────┴───────────────────────────┴───────────────────────────┘
```

- 宏名称可重命名（双击或「⋯」菜单）。
- 「⋯」菜单：重命名 / 复制 / 删除 / 导入 / 导出。

### 4.3 新建/编辑宏 - 编辑器（右侧抽屉）

- **触发**：点「新建宏」或选中宏后点「编辑」。
- **形态**：右侧滑出抽屉式面板（约主区 40% 宽），主区其余部分遮罩/留白。

```
┌───────────────────────────────────────────────────────────┐
│ ×  [宏名称✏️]                    [开始录制][重置][保存]     │  ← 面板头部
├───────────────────────────────────────────────────────────┤
│ 播放方式                                                    │
│ [循环次数播放] [任意键停止播放] [按住循环]  循环 [ 1 ] 次    │  ← 三选一卡片
├───────────────────────────────────────────────────────────┤
│ ⌨ 插入键盘按键      🖱 插入鼠标按键                          │  ← 插入动作入口
├───────────────────────────────────────────────────────────┤
│ 按键    动作     延迟(设备实际)                             │
│ ───────────────────────────────────────                   │
│ Q      按下     [10ms]   [↑][↓][×]                        │
│ 左键    抬起     [25ms]   [↑][↓][×]                       │
│ ...（序列编辑区，键盘/鼠标步骤可混合）                      │
└───────────────────────────────────────────────────────────┘
```

### 4.4 面板组件

| 区域 | 元素 | 视觉 | 文案 | 作用 |
|------|------|------|------|------|
| 头部 | 关闭 | 左侧 × 图标 | — | 关闭抽屉 |
| 头部 | 宏名称 | 黑色文字 + 编辑铅笔图标 | 默认「宏1」 | 重命名当前宏 |
| 头部 | 录制 | 圆角描边按钮，前置圆点 | 「开始录制」 | 录制按键序列；录制中变「停止录制」（红底呼吸闪烁）+ 隐藏插入按钮 |
| 头部 | 重置 | 圆角描边按钮 | 「重置」 | 清空当前序列 |
| 头部 | 保存 | 黑色实心主按钮 | 「保存」 | 保存宏 |
| 播放方式 | 分段选择 | 黑色高亮当前项 | `循环次数播放` / `任意键停止播放` / `按住循环` | 三种播放方式 |
| 播放方式 | 循环次数 | 数字输入框 | `循环 1 次` | 仅「循环次数播放」方式下启用 |
| 插入 | 键盘按键 | 图标 + 文字按钮 | 「插入键盘按键」 | 插入键盘动作步骤 |
| 插入 | 鼠标按键 | 图标 + 文字按钮 | 「插入鼠标按键」 | 插入鼠标动作步骤（左键/右键/中键） |
| 序列 | 单步 | 一行三列 | 按键/动作/延迟 | 支持上移/下移/删除（行内 [↑][↓][×]） |

### 4.5 单步（步骤）数据结构

| 字段 | 取值 | 说明 |
|------|------|------|
| 按键 | 键盘键名 / 鼠标按键名 | 如 Q、左键、右键… |
| 动作 | 按下 / 抬起 | 录制时自动识别，可手动修改 |
| 延迟 | 数字（设备实际生效延迟） | 步骤间毫秒数，可编辑 |

> **延迟语义（协议事实）**：UI 上「延迟」输入框按**设备实际生效延迟**标注（不是 PC 输入值），`PcInputToActualMs` 换算已封装在代码层；列表上方常驻提示「延迟为设备实际生效值」。

### 4.6 录制交互

- 点「开始录制」→ 按钮变「停止录制」（红色呼吸）+ 隐藏「插入键盘/插入鼠标」按钮。
- 录制期间操作的鼠标/键盘按键，其「按下/抬起」由系统自动识别，结果可在序列中手动修改。
- 点「停止录制」→ 序列自动回填，恢复插入按钮。

### 4.7 保存闭环

```
快捷指令(空状态) → 新建宏 → 插入键盘/鼠标步骤 → 保存
  → Toast「宏已保存」→ 回到宏列表（含 宏1）
  → 选中 宏1 → 右侧摘要 → 编辑 → 回到编辑器
```

- 「复制」：自动保存当前宏并复制整条步骤序列，生成新宏（如「宏1_copy」）。
- **未绑定警告**：写宏前若该宏未被任何按键引用，提示「此宏尚未绑定到按键，可在改键设置页绑定」。

### 4.8 对产品参考意义

- 空状态（灰字说明 + 黑色主按钮）直接复用。
- 编辑器「槽位/命名 + 播放方式 + 序列区 + 录制/插入工具条 + 保存」结构完整复用；参考的「AI 生成」按钮**不纳入**本产品（无此能力）。
- 键盘/鼠标步骤混合的序列数据模型可直接复用。

---

## 五、页面：DPI设置（DPI Levels）

> 对应参考「DPI设置」，承载 DPI 挡位功能。依据官方 Wiki 与 0x04 协议：5 档（6-8 软件隐藏），范围 40~4800、步进 40，默认 800/1200/1600/2400/4000，指示灯颜色 红/绿/蓝/紫/黄。

- **触发方式**：左侧菜单 → DPI设置。
- **默认启动页**：本产品首屏为「改键设置」（对齐参考）；如产品确认以 DPI 为最高频入口可改（待定，见 5.5）。

### 5.1 布局结构

```
┌────────┬─────────────────────────────────────────────────┐
│ 86%    │  [ DPI设置 · 调整鼠标移动的灵敏速度 ]              │  ← 标题栏
│ M618XSD│                                                 │
│ 日常 ▼ │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ ────── │  │ DPI 1  │ │ DPI 2  │ │ DPI 3  │ │ DPI 4  │ │ DPI 5  │
│ 改键设置│  │ ●红色  │ │ ○绿色  │ │ ○蓝色  │ │ ○紫色  │ │ ○黄色  │
│ 快捷指令│  │[ 800 ] │ │[ 1200 ]│ │[ 1600 ]│ │[ 2400 ]│ │[ 4000 ]│
│ DPI设置 │  │ [启用]  │ │ [启用]  │ │ [启用]  │ │ [启用]  │ │ [启用]  │
│ 性能设置│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘
│ 参数设置│  当前档位：第 3 档 · 1600 DPI                    │
│ 配置管理│                                                 │
│ 其他设置│                  [ 待保存 ]                     │
└────────┴─────────────────────────────────────────────────┘
```

### 5.2 卡片内容（每档一张）

| 元素 | 样式 | 说明 |
|------|------|------|
| 档位指示灯 | 色块（红/绿/蓝/紫/黄），当前档位点亮 | 仅展示，不可改色（该鼠标无 RGB 指示灯功能） |
| 数值输入框 | 数字输入（最多 5 位） | 范围 40-4800 |
| 启用勾选 | 复选框 | 关闭该档后 DPI 键切换跳过它 |
| 设为当前 | 单选按钮 | 该档变为活跃档位 |

### 5.3 校验与反馈

- 输入校验：40-4800，步进 40 的倍数；非倍数实时红框 + tooltip「建议 40 的倍数」，非法值不写入。
- 保存成功 Toast「已保存，鼠标 OLED 已切换」。
- **切档即时生效**：点击「设为当前」即写设备；底部「当前档位：第 X 档 · XXXX DPI」实时跟随（硬件按 DPI 键切档时通过 Input Report 自动同步高亮）。

### 5.4 对产品参考意义

- 参考 DPI 页为 8 个垂直滑块；本产品改**横向卡片式**，每档「数值 + 启用 + 设为当前」一目了然，对不懂 DPI 的用户更直观。
- 协议限制（当前档位不可主动读）→ UI 不提供「读取档位」按钮，靠「写入 + 本地记忆 + Input 上报同步」。

### 5.5 已知问题 / 待确认

- 三阶段计划文档指定「默认启动页 = DPI」，参考原型首屏为「改键设置」。**启动页待产品确认**（原型按参考取「改键设置」）。

---

## 六、页面：性能设置（Polling Rate）

> 对应参考「性能设置」，承载回报率 + 去抖功能。依据官方 Wiki 与 0x06 协议：4 档 125/250/500/1000 Hz（idx=1000÷Hz）。

- **触发方式**：左侧菜单 → 性能设置。
- **当前状态**：已连接，展示圆形仪表盘选择器 + 去抖滑块。

### 6.1 布局结构

```
┌────────┬─────────────────────────────────────────────────┐
│ 86%    │  [ 性能设置 · 调整鼠标与电脑的通信频率 ]              │  ← 标题栏
│ M618XSD│                                                 │
│ 日常 ▼ │          ┌───────────────┐                       │
│ ────── │         │   125 Hz       │                       │
│ 改键设置│        │  ┌───────────┐  │                      │
│ 快捷指令│        │  │ 游戏模式  │  │                       │
│ DPI设置 │        │  │  (500Hz)  │  │                       │
│ 性能设置│         │   1000 Hz   │                         │
│ 参数设置│       └───────────────┘                       │
│ 配置管理│                                                 │
│ 其他设置│  ┌────────────────────────────────────────┐    │
│        │  │ 按键响应时间（去抖）                       │    │
│        │  │ [━━━━━━━━━━○━━━━]  6 ms   [1~25ms]      │    │
│        │  │ 数值越小响应越快，误触概率越高              │    │
│        │  └────────────────────────────────────────┘    │
│        │                   [ 待保存 ]                    │
└────────┴─────────────────────────────────────────────────┘
```

### 6.2 回报率仪表盘

- 圆形仪表盘式选择器，四象限分别对应 125/250/500/1000 Hz。
- 中心显示当前模式名：

| 回报率 | 模式名 |
|--------|--------|
| 125 Hz | 省电模式 |
| 250 Hz | 办公模式 |
| 500 Hz | 游戏模式 |
| 1000 Hz | 电竞模式 |

- 选中象限高亮，中心同步模式名；点击即选中，停止操作约 1.5 秒后自动保存。

### 6.3 按键响应时间（去抖）

- 水平滑块 + 数值显示，范围 1-25ms，默认 6ms。
- 旁注「数值越小响应越快，误触概率越高」。
- 说明：去抖值包含在灯光+电源报告（0x05）中，与灯光页共用一条写入链路，UI 上独立成卡片。

### 6.4 对产品参考意义

- 仪表盘四象限 + 中心模式名与官方同款，普通用户「看中心就知道现在什么档位」，直接复用。
- 去抖滑块实时显示数值 + 一句人话说明，避免「去抖是啥」的困惑。

---

## 七、页面：参数设置（Light & Power）

> 对应参考「参数设置」，承载灯光与电源功能。依据官方 Wiki 与 0x05 协议：5 种灯效 + 速度 + 移动关灯 + 电源管理（一级休眠/睡眠）。

- **触发方式**：左侧菜单 → 参数设置。

### 7.1 布局结构

```
┌────────┬─────────────────────────────────────────────────┐
│ 86%    │  [ 参数设置 · 调整鼠标灯光与休眠行为 ]            │  ← 标题栏
│ M618XSD│                                                 │
│ 日常 ▼ │  ⚠ 红色风险条（见 7.3）                          │
│ ────── │                                                 │
│ 改键设置│  ┌────────── 灯光 ──────────┐                   │
│ 快捷指令│  │  模式：呼吸DPI  ▼          │                   │
│ DPI设置 │  │  速度：[━━━●━━━━━━] 5/8   │                   │
│ 性能设置│  │  ☐ 移动时关闭灯光           │                   │
│ 参数设置│  └───────────────────────────┘                   │
│ 配置管理│                                                 │
│ 其他设置│  ┌────── 电源管理（风险区）────┐                   │
│        │  │  一级休眠  [━━○━━] 2分钟    │                   │
│        │  │  睡眠时间  [━━○━━] 20分钟   │                   │
│        │  │  ( ) 不修改，使用默认值（推荐）│                  │
│        │  └───────────────────────────┘                   │
│        │                   [ 待保存 ]                     │
└────────┴─────────────────────────────────────────────────┘
```

### 7.2 灯光卡片

| 元素 | 样式 | 说明 |
|------|------|------|
| 灯效模式 | 下拉框或 5 张模式图标卡片 | 关闭 / 呼吸DPI / 常亮DPI / 循环呼吸 / 霓虹 |
| 速度 | 水平滑块 1-8 | 呼吸/循环速度，实时显示数值 |
| 移动时关闭灯光 | 复选框 | 手移动鼠标时关闭 RGB 灯效 |

### 7.3 电源管理（红色风险区）

- 顶部红色警示条：「固件对自定义电源命令反应不稳定，有断联风险，建议使用官方软件操作」。
- 默认选中「不修改，使用默认值（推荐）」单选项——即本产品**默认不碰电源字段**。
- 若用户选择修改：先检测数据设备已打开（静默），弹二次确认「电源设置可能导致鼠标断联，确认继续？」，再写入。
- 一级休眠滑块（0.5~30 分钟）、睡眠时间滑块（1~60 分钟）。

### 7.4 对产品参考意义

- 参考「参数设置」页（灯光+电源）分区清晰；本产品将灯光与电源分卡片、电源降级为风险区并默认不修改，把不可靠的协议能力挡在普通用户操作之外。
- 灯光模式图标化（呼吸/常亮/循环/霓虹各用图标），比纯下拉直观。

### 7.5 已知问题 / 待确认

- 参考中「灯光亮度」「16 种预设色 + RGB 手动输入」在协议中**该鼠标无此功能**（AGENTSK 6 节），本页不展示颜色面板与亮度。
- 电源字段与去抖共用 0x05 报告，写入时打包一次发送；UI 上独立卡片、发送链路共享。

---

## 八、页面：配置管理（Profile）

> 对应参考「配置管理」。本产品**该页尚未实现**（三阶段计划文档 Phase 7），原型先行；设备无配置槽位，Profile 为纯软件层，切换 = 全量写全套报告。

- **触发方式**：左侧菜单 → 配置管理。
- **当前状态**：本页标注「开发中」角标，交互按规划设计。

### 8.1 布局结构

```
┌────────┬─────────────────────────────────────────────────┐
│ 86%    │  [ 配置管理 · 保存和切换整套鼠标设置 ]              │  ← 标题栏
│ M618XSD│  ⓘ 本页开发中，以下为规划原型                      │
│ 日常 ▼ │  ┌──────────┬───────────────────────────────┐    │
│ ────── │  │ 配置列表  │  当前配置摘要                  │    │
│ 改键设置│  │ ├ 日常●   │  · DPI：800/1200/1600/2400/4000│   │
│ 快捷指令│  │ ├ 游戏    │  · 回报率：游戏模式(500Hz)     │    │
│ DPI设置 │  │ └ ...    │  · 灯光：呼吸DPI / 速度5       │    │
│ 性能设置│  │ [＋][−]  │  · 按键：5 项已自定义           │    │
│ 参数设置│ │ [✎][↑][↓]│  · 宏：2 个                   │    │
│ 配置管理│  └──────────┴───────────────────────────────┘    │
│ 其他设置│                                                 │
│        │                     [ 待保存 ]                    │
└────────┴─────────────────────────────────────────────────┘
```

### 8.2 配置列表管理

| 按钮 | 功能 |
|------|------|
| ＋ | 新建配置（向导：命名 → 从当前设备复制 / 从默认值开始） |
| － | 删除选中的配置（二次确认；「日常/游戏」内置配置可删可重建） |
| ✎ | 编辑配置名称 |
| ↑ / ↓ | 上移 / 下移配置顺序 |
| 点击配置项 | 选中并预览右侧摘要 |

### 8.3 切换流程

- 手动切换：选中 → 全量写入 → 进度条「正在应用第 3/8 步…」（0x0C→0x04→0x05→0x06→0x08→0x09×3 逐步推进）。
- 失败中断：Toast「应用失败，已回滚到上一个配置」。
- 删除/覆盖二次确认。

### 8.4 配置内容（每套保存 9 模块全套值）

DPI 数值+启用+活跃档位 / 灯光模式+速度+移动关灯 / 回报率 / 去抖 / 按键映射全表 / 宏（含绑定关系）/ 电源管理 / 电池（仅展示）。

### 8.5 对产品参考意义

- 左右分栏（列表 + 摘要卡片）+ 底部操作按钮与官方同构，直接复用。
- 切换进度可见、失败可回滚，避免「点了没反应」和「切了一半」的焦虑。

---

## 九、页面：其他设置（Other Settings）

> 对应参考「其他设置」。内容：**电量、产品外观选择（黑/白）、恢复出厂设置、官网驱动下载、关于**。

- **触发方式**：左侧菜单 → 其他设置（或顶部 ⚙ 齿轮）。

### 9.1 布局结构

```
┌────────┬─────────────────────────────────────────────────┐
│ 86%    │  [ 其他设置 · 电量、外观与维护 ]                    │  ← 标题栏
│ M618XSD│                                                 │
│ 日常 ▼ │  ┌────────── 电池 ──────────┐                   │
│ ────── │  │  [███▓░░░░░]  86%        │                   │
│ 改键设置│  │  状态：未充电 / 无线使用中 │                   │
│ 快捷指令│  └───────────────────────────┘                   │
│ DPI设置 │  ┌────────── 产品外观 ───────┐                   │
│ 性能设置│  │  (●) 黑色   ( ) 白色      │                   │
│ 参数设置│  │  影响全站鼠标示意图配色    │                  │
│ 配置管理│  └───────────────────────────┘                   │
│ 其他设置│  ┌────────── 维护 ───────────┐                   │
│        │  │  [ 恢复出厂设置 ]（红色危险按钮）│                │
│        │  └───────────────────────────┘                   │
│        │  ┌────────── 官方驱动 ────────┐                   │
│        │  │  如需官方软件管理电源等高级功能，│                 │
│        │  │  [ 打开官网驱动下载页 ]     │                   │
│        │  └───────────────────────────┘                   │
│        │  ┌────────── 关于 ────────────┐                   │
│        │  │  DELUX M618XSD 客户端 v0.1.0 │                 │
│        │  │  非官方软件，仅作个人使用      │                 │
│        │  └───────────────────────────┘                   │
└────────┴─────────────────────────────────────────────────┘
```

### 9.2 电池卡片

- 电池图标式进度条（红→绿渐变）+ 百分比，实时刷新（监听 Input Report，约 2s 更新一次）。
- 状态文案：未充电 / 充电中 / 已充满（满电挂线）/ 充电中（插入检测）。

### 9.3 产品外观卡片

- 黑 / 白 二选一（单选卡片，附两种外观的鼠标缩略图）。
- 影响全站鼠标示意图配色（等待页插画、改键设置页示意图）。
- 纯本地设置，本地持久化，不写入设备。

### 9.4 维护卡片（恢复出厂设置）

- 红色危险按钮 + 说明「将把 DPI、灯光、回报率、去抖、按键映射、宏全部恢复为出厂默认值」。
- 二次确认弹窗 + 提示「请先关闭官方驱动」。
- 协议无硬件出厂重置命令（事实），恢复出厂 = 软件默认值覆写全套报告。

### 9.5 官方驱动卡片

- 文案「如需官方软件管理电源等高级功能，请访问官网下载」。
- 按钮「打开官网驱动下载页」→ 浏览器打开官方下载链接（URL 待产品确认）。
- 作为官方软件入口的补充，与电源页「建议官方操作」呼应。

### 9.6 关于卡片

- 产品名、版本号、免责声明「非官方软件，仅作个人使用」。
- 版本号与 exe 构建时间戳一致（读取程序集版本）。

### 9.7 对产品参考意义

- 参考「其他设置」为杂项入口；本产品将其组织为 **电池 / 外观 / 维护 / 官方驱动 / 关于** 五张卡片，信息分层清晰。
- 恢复出厂设置作为唯一「重操作」用红色危险按钮 + 二次确认，放在不打扰日常的「维护」卡片里。

---

## 十、页面清单

| # | 页面 | 状态 | 承载功能 |
|---|------|------|---------|
| 1 | 连接设备等待页 | ✅ 原型 | — |
| 2 | 设备配置页 - 改键设置 | ✅ 原型 | 按键映射 |
| 3 | 改键设置 - 动作分配面板 | ✅ 原型 | 按键功能选择 |
| 4 | 快捷指令（含编辑器） | ✅ 原型 | 宏管理 |
| 5 | DPI设置 | ✅ 原型（自研） | DPI 挡位 |
| 6 | 性能设置 | ✅ 原型（自研） | 回报率 + 去抖 |
| 7 | 参数设置 | ✅ 原型（自研） | 灯光与电源 |
| 8 | 配置管理 | ⏳ 未实现，原型先行 | 配置管理 |
| 9 | 其他设置 | ✅ 原型（自研） | 电量/外观/恢复出厂/官网/关于 |

---

## 十一、待产品确认事项

| # | 事项 | 选项/建议 |
|---|------|----------|
| 1 | 启动默认页 | 改键设置（参考）vs DPI设置（三阶段计划文档指定） |
| 2 | 官网驱动下载链接 URL | 待提供官方地址后填入 |
| 3 | 配置管理页实现排期 | 三阶段计划文档 Phase 7 |
| 4 | 产品外观（黑/白）默认值 | 默认黑色，用户可在其他设置修改 |
| 5 | 帮助与反馈页 | 本轮未展开，后续按 Phase 8「帮助/FAQ」补充 |

---

## 十二、补充记录模板（每新增一页时复制此结构）

```
## N、页面：<页面名>
- 对应参考：
- 触发方式：
- 当前状态：
### 布局结构
（ASCII 或文字描述）
### 组件清单
| 位置 | 元素 | 视觉 | 文案 | 改造点 |
### 备注 / 待确认
```
