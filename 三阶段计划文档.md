# DELUX M618XSD 鼠标客户端 — 0→1 计划文档

> 目标：基于现有 `MouseDriverClient` 代码基础，构建一款用户体验良好的正式客户端软件。

---

## 一、项目定位

**产品名称**：DELUX M618XSD 鼠标客户端
**性质**：C 端用户软件（非调试工具、非驱动）
**目标用户**：M618XSD 鼠标普通用户
**核心价值**：替代官方 Mouse.exe，提供更简洁、更可控的配置体验

---

## 二、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| **UI 框架** | WPF（原生） + **WPF-UI** NuGet | Fluent 风格，自带导航控件、主题切换 |
| **MVVM 工具** | CommunityToolkit.Mvvm | `[ObservableProperty]` / `[RelayCommand]` 源码生成器 |
| **DI 容器** | Microsoft.Extensions.DependencyInjection | 注入服务到 ViewModel |
| **HID 通信** | 复用现有 `HidComm.cs` / `HidNative.cs` | P/Invoke hid.dll，无需改动 |
| **数据模型** | 复用现有 `Models.cs` | DpiConfig / ButtonConfig / MacroConfig |

**不引入**：Serilog、日志库、Material 风格控件。保持单技术栈，无冗余依赖。

---

## 二、项目结构

```
DELUX.Driver/
├── App.xaml / App.xaml.cs          # WPF-UI 初始化 + DI 注册
├── App.config                      # DPI 感知声明
│
├── Services/
│   └── HidService.cs               # 封装 HidComm，提供 async 方法
│
├── ViewModels/
│   ├── MainViewModel.cs            # 导航状态管理
│   ├── DpiViewModel.cs             # DPI 页面逻辑
│   ├── ButtonViewModel.cs          # 按键映射页面逻辑
│   └── MacroViewModel.cs           # 宏管理页面逻辑
│
├── Views/
│   ├── MainWindow.xaml             # FluentWindow 主窗口（含侧边导航）
│   └── Pages/
│       ├── DpiPage.xaml            # DPI 配置页
│       ├── ButtonPage.xaml         # 按键映射页
│       └── MacroPage.xaml          # 宏管理页
│
└── Models/
    └── （直接引用 MouseDriverClient/Models.cs 的复制件）
```

---

## 三、分阶段实施计划

### Phase 1：项目搭建 + 基础导航（约 0.5 天）

**目标**：能启动程序，看到 WPF-UI 导航框架跑通。

- [ ] `dotnet new wpf -n DELUX.Driver -f net8.0-windows`
- [ ] `Install-Package Wpf.Ui -Version latest`
- [ ] `Install-Package CommunityToolkit.Mvvm`
- [ ] `Install-Package Microsoft.Extensions.DependencyInjection`
- [ ] 迁移 `HidComm.cs` / `HidNative.cs` / `Models.cs` 到新建项目
- [ ] 编写 `App.xaml`：注册 WPF-UI 主题（Modern，浅色）
- [ ] 编写 `MainWindow.xaml`：使用 `ui:FluentWindow` + `ui:NavigationView`
- [ ] 添加三个空白 Page 占位，验证导航跳转正常
- [ ] `App.xaml.cs`：注册 DI 容器，注入 MainViewModel

### Phase 2：设备通信层封装（约 0.5 天）

**目标**：ViewModel 能通过服务层与鼠标通信，不直接操作 HidComm。

- [ ] 编写 `HidService.cs`：
  - `ConnectAsync()` / `Disconnect()`
  - `ReadDpiConfigAsync()` → 返回 `DpiConfig`
  - `WriteDpiConfigAsync(DpiConfig)` → 返回 bool
  - `WriteButtonConfigAsync(ButtonConfig)` → 返回 bool
  - `BuildMacroSequenceAsync(...)` → 返回 `byte[][]`
  - Input Report 监听：`StartListener()` / `StopListener()`
  - 事件：`OnDpiLevelChanged(byte level)` 供 ViewModel 订阅
- [ ] 在 `App.xaml.cs` 中注册 `HidService` 为单例
- [ ] `MainViewModel` 持有连接状态（Connected / Disconnected），绑定到 UI 状态灯

### Phase 3：DPI 页面（约 0.5 天）

**目标**：读取/写入 DPI 配置，UI 实时响应硬件切档。

- [ ] `DpiViewModel.cs`：
  - `[ObservableProperty] public List<DpiLevelModel> Levels`（含 Value/Enabled/IsActive）
  - `[RelayCommand] LoadConfig()` → 调 `HidService.ReadDpiConfigAsync()`
  - `[RelayCommand] ApplyConfig()` → 序列化写入 + 激活位图更新
  - `[RelayCommand] SwitchLevel(byte level)` → 只写 `[24]`，轻量切档
  - 订阅 `HidService.DpiLevelChanged` → 更新 `ActiveLevel` + UI 高亮
- [ ] `DpiPage.xaml`：
  - 顶部：「加载配置」+「应用」+「切档」按钮
  - 中间：`ItemsControl` 渲染 8 个档位（数值输入框 + 启用勾选 + 激活单选）
  - 底部：当前档位状态文字
- [ ] 接入 WPF-UI 卡片/分隔样式，视觉统一

### Phase 4：按键映射页面（约 0.5 天）

**目标**：选择按钮 → 设置功能 → 应用写入，防止误清空整表。

- [ ] `ButtonViewModel.cs`：
  - 下拉框数据源：18 个按钮名称数组（来源：AGENTS.md 映射表）
  - 功能编码下拉框：标准/左键/右键/中键/前进/后退/DPI循环/宏/滚轮方向 等
  - `[RelayCommand] LoadConfig()` / `[RelayCommand] ApplyConfig()`
  - 写入时：克隆默认表 → 修改目标 entry → 整体发送（避免清空其余按钮）
- [ ] `ButtonPage.xaml`：
  - 两列布局：左侧按钮列表（或下拉），右侧功能选择区
  - 当前 3 字节原始值显示区（Consolas 字体）
  - 「应用」按钮

### Phase 5：宏管理页面（约 1 天）

**目标**：录制动作序列 → 选绑按钮 → 写入设备。

- [ ] `MacroViewModel.cs`：
  - `Actions` 集合（每项 = keycode + flag）
  - 动作类型枚举：键盘键 / 鼠标键 / 延时 / 按下 / 释放
  - `[RelayCommand] AddKeyAction()` / `AddDelayAction()` / `DeleteLastAction()` / `ClearActions()`
  - `[RelayCommand] WriteMacro()` → 调用 `MacroConfig.BuildLightweightSequence()` 逐块发送
  - 播放方式下拉（循环次数 / 任意键停止 / 按住循环）
- [ ] `MacroPage.xaml`：
  - 动作列表（ListBox，带删除按钮）
  - 添加动作工具栏（键盘输入 + 延时输入）
  - 宏 ID 选择 + 播放方式选择
  - 绑定按钮下拉 + 「写入」按钮
  - 原始字节预览区

### Phase 6：打磨发布（约 0.5 天）

- [ ] 程序图标（替换默认 WPF 图标）
- [ ] 设置窗口标题、最小尺寸、禁止缩放（避免布局崩坏）
- [ ] 添加关于对话框（版本号、品牌信息）
- [ ] 连接失败提示（MessageBox 友好文案：「请关闭官方 Mouse.exe 后重试」）
- [ ] `dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`
- [ ] 测试：断连恢复、热插拔、各页面切换流畅度

---

## 四、已知风险 & 应对

| 风险 | 应对 |
|---|---|
| WPF-UI 与现有 `HidNative.cs` P/Invoke 不兼容 | 两者独立，无冲突；HidNative 是纯静态类 |
| 断连后 ViewModel 状态残留 | 断开时重置 `_isConnected`，UI 禁用所有操作按钮 |
| 宏写入时 Mouse.exe 未退出导致断连 | 连接前检测进程，弹出明确提示 |
| 用户改错配置 → 设备异常 | 每页「加载配置」按钮可回滚到设备当前值 |

---

## 五、验收标准

- [ ] 程序启动后自动检测 2.4G 接收器，显示连接状态
- [ ] DPI 页：读取设备配置 → 修改数值 → 应用 → OLED 生效；按鼠标 DPI 键 → UI 同步
- [ ] 按键页：选按钮改功能 → 应用 → 实机按键生效（对照 AGENTS.md 映射验证）
- [ ] 宏页：录制 3-5 步动作 → 写入 → 实机触发宏
- [ ] 单文件发布包可脱离 .NET 运行环境直接运行
