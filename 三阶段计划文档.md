# DELUX M618XSD 鼠标客户端 — 0→1 计划文档

> 目标：基于现有 `MouseDriverClient` 代码基础，构建一款**面向普通用户**、体验良好的正式客户端软件。
> 本文档是 Phase 3 的权威实施依据；协议事实、风险与前置条件以 `reference/` 文档为准，本文不再重复。
> **UI 蓝本**：各页「长什么样、怎么交互」以 `M618XSD客户端原型描述.md` 为准（其承载 9 个页面的完整原型，含**其他设置页**、连接等待页、侧边栏导航等本计划未细画的视觉规格）。本计划定「做什么/验收」，原型定「长什么样/怎么用」，二者互为补充、冲突以协议事实与用户体验优先裁决。

---

## 一、项目定位

| 项 | 内容 |
|---|---|
| **产品名称** | DELUX M618XSD 鼠标客户端 |
| **性质** | C 端用户软件（非调试工具、非驱动、非开发者工具） |
| **目标用户** | M618XSD 鼠标的普通用户（多数不懂 DPI/回报率/HID，只想「改个键、调个灯、存几套配置」） |
| **核心价值** | 替代官方 Mouse.exe：更简洁、更可控、更清晰的配置体验；官方软件的断联/不稳定问题由我们规避 |
| **成功标准** | 用户**无需看说明书**即可完成 90% 的日常配置；危险操作有明确保护；失败时有可执行的引导 |

### 1.1 用户画像与典型场景

- **场景 A（新用户，配置一次）**：装上鼠标 → 打开客户端 → 看到「已连接」→ 调 DPI、关灯 → 退出。全程 < 2 分钟。
- **场景 B（游戏用户）**：绑定侧键为宏/快捷键，切换游戏 Profile。
- **场景 C（办公用户）**：设回报率、去抖、调音量快捷键、看电池电量。
- **场景 D（进阶）**：编辑/录制宏、导入导出配置、管理多套 Profile。

### 1.2 UX 目标（对照原则逐条可验收）

1. **所见即所得**：UI 上的每一项修改，设备真实生效前都有「待保存」提示；自动保存成功后有成功反馈（防抖合并：停止操作约 1.5 秒自动写入）。
2. **零猜测**：所有协议限制（如 0x08 整表覆写、电源断联风险）转译为 UI 文案，不暴露技术细节。
3. **防呆**：危险操作（整表覆写、电源命令、重置）二次确认。
4. **即时反馈**：连接/断连/保存/错误均有状态提示（顶部状态条 + 页面级 Toast）。
5. **可逆**：改错可撤销——「恢复出厂」仅在其他设置提供；未保存编辑可放弃（断连/切页前确认），改错不丢已保存配置。
6. **一致**：所有页面统一布局、统一控件样式、统一文案语气。

---

## 二、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| **UI 框架** | WPF（**纯原生，无第三方 UI 库**） | 自研浅/深主题资源字典（`Themes/`）+ 原生控件 + `Segoe MDL2 Assets` 图标，风格对齐 WPF Gallery |
| **MVVM 工具** | CommunityToolkit.Mvvm | `[ObservableProperty]` / `[RelayCommand]` 源码生成器 |
| **DI 容器** | Microsoft.Extensions.DependencyInjection | 注入服务到 ViewModel |
| **HID 通信** | 复用现有 `HidComm.cs` / `HidNative.cs` | P/Invoke hid.dll，无需改动 |
| **数据模型** | 复用现有 `Models.cs` | DpiConfig / ButtonConfig / MacroConfig / LightConfig / RateConfig |
| **单元测试** | xUnit（复用 `MouseDriverClient.Tests` 模式） | 协议编解码在 Linux 交叉编译 + Windows 实机跑 |

**不引入**：Serilog、日志库、Material 风格控件、任何未列入本计划的第三方 UI 库。保持单技术栈，无冗余依赖。

---

## 三、项目结构

```
DELUX.Driver/
├── App.xaml / App.xaml.cs          # 原生 WPF 主题初始化（浅/深资源字典切换） + DI 注册 + 全局异常钩子(crash.log)
├── App.config                      # DPI 感知声明（PerMonitorV2）
│
├── Services/
│   ├── HidService.cs               # 封装 HidComm，async 方法 + Input Report 事件
│   ├── ProfileStore.cs             # Profile 的 JSON 持久化（9 模块全套值）
│   └── DialogService.cs            # 统一的消息/确认/输入对话框（可测试）
│
├── ViewModels/
│   ├── MainViewModel.cs            # 导航 + 连接状态 + 全局自动保存（防抖）/恢复出厂编排
│   ├── DpiViewModel.cs
│   ├── ButtonViewModel.cs
│   ├── MacroViewModel.cs
│   ├── LightViewModel.cs           # 灯光 + 电源管理（风险提示）
│   ├── RateViewModel.cs            # 回报率 + 去抖
│   ├── BatteryViewModel.cs
│   ├── ProfileViewModel.cs
│   └── OtherSettingsViewModel.cs  # 其他设置页：电量/外观/恢复出厂/官方驱动/关于（蓝本见原型文档第九节）
│
├── Views/
│   ├── MainWindow.xaml             # 原生 Window 主窗口（标题栏 + 侧边 ListBox 导航 + 内容区）
│   ├── Controls/                   # 复用控件（DPI 滑块卡片、电池图标、仪表盘选择器）
│   └── Pages/
│       ├── DpiPage.xaml
│       ├── ButtonPage.xaml
│       ├── MacroPage.xaml
│       ├── LightPage.xaml          # 灯光 + 电源
│       ├── RatePage.xaml           # 回报率 + 去抖
│       ├── BatteryPage.xaml
│       ├── ProfilePage.xaml
│       └── OtherSettingsPage.xaml  # 其他设置：电池(展示)/外观/恢复出厂/官方驱动/关于
│
└── Models/
    └── （直接引用 MouseDriverClient/Models.cs 的复制件，或改为共享项目）
```

---

## 四、分阶段实施计划

> 每个 Phase 同时包含 **功能** 与 **UX/UI** 两个维度，缺一不可。
> 所有页面遵循统一的「页面骨架」：页面标题（页名 + 说明）→ 配置区（卡片）→ 自动保存（防抖 1.5s）。「恢复出厂」仅在「其他设置」页提供。

### Phase 1：项目搭建 + 基础导航（约 0.5 天）

**目标**：能启动程序，看到原生 WPF 导航框架跑通。

**功能**
- [ ] `dotnet new wpf -n DELUX.Driver -f net8.0-windows`
- [ ] 安装 `CommunityToolkit.Mvvm`、`Microsoft.Extensions.DependencyInjection`（**不引入任何第三方 UI 库**）
- [ ] 迁移 `HidComm.cs` / `HidNative.cs` / `Models.cs` 到新建项目
- [ ] `App.xaml`：原生浅/深主题资源字典（`Themes/`）；`App.xaml.cs`：DI 注册 + 未处理异常 → `crash.log`
- [ ] `MainWindow.xaml`：原生 `Window` + `ListBox` 侧边导航（无 WPF-UI）
- [ ] 八个空白 Page 占位（DPI/按键/宏/灯光/回报率/电池/配置管理/其他设置），验证导航跳转；
      导航分组与页面命名对齐原型文档侧边栏（改键设置/快捷指令/DPI设置/性能设置/参数设置/配置管理/其他设置）

**UX/UI**
- [ ] 窗口标题「DELUX M618XSD」，尺寸 ≥ 900×640，最小尺寸锁定，禁止缩放变形
- [ ] 侧边导航分组：**设备状态**（电池）/ **设置**（DPI/按键/宏/灯光/回报率）/ **管理**（配置）
- [ ] 顶部常驻「连接状态条」：绿点=已连接 / 灰点=未连接 / 黄点=连接中，旁带「连接/断开」按钮
- [ ] 空态：未连接时各页面显示统一引导卡片「连接鼠标后开始配置」，操作按钮禁用
- [ ] 默认启动页 = DPI（最高频配置）；导航项带图标

### Phase 2：设备通信层封装（约 0.5 天）

**目标**：ViewModel 通过服务层与鼠标通信，不直接操作 HidComm。

**功能**
- [ ] `HidService.cs`：`ConnectAsync` / `Disconnect` / 各配置的读-写 / `StartListener` / `StopListener`
- [ ] 事件：`OnDpiLevelChanged(byte)`、`OnBatteryChanged(byte)`、`OnConnectionChanged(bool)`
- [ ] `App.xaml.cs` 注册 `HidService` 为单例；`MainViewModel` 持有连接状态
- [ ] 启动时自动尝试连接（后台线程，超时 3s，失败不阻塞 UI）
- [ ] 发送时序集中封装：`SendSequence(params byte[][])` 统一 0.2s 间隔（0x0C→0x08→0x09 每步 0.2s，见 AGENTSK 2.3d）

**UX/UI**
- [ ] 连接失败提示：区分「未插接收器」「需退出官方 Mouse.exe」「设备被占用」，各自给可执行文案
- [ ] 连接成功后若检测到 Mouse.exe 进程 → 顶部黄色警告条「检测到官方驱动运行中，为避免冲突请先关闭」，一键「打开任务管理器」按钮
- [ ] 断连时：页面操作全部禁用 + 顶部状态条变灰 + 已修改但未保存的内容**保留**（不丢用户输入），重连后自动保存

### Phase 3：DPI 页面（约 0.5 天）

**目标**：读写 DPI 配置，UI 实时响应硬件切档。

**功能**
- [ ] `DpiViewModel`：8 槽位（仅前 5 暴露，6-8 隐藏）、`LoadConfig` / `SaveConfig`（防抖） / `SwitchLevel`
- [ ] 订阅 `OnDpiLevelChanged` → 同步活跃档位高亮（硬件切档跟随）
- [ ] 保存后本地记忆 `ActiveLevel`；不主动 GetFeature 读档位（`[24]` 恒 0，见 AGENTSK 2.4）

**UX/UI**
- [ ] 每档一张卡片：数值输入框 + 启用勾选 + 「设为当前」单选 + 档位指示灯色块
- [ ] 输入校验：40-4800、步进 40 的倍数（非倍数时实时红框 + tooltip「建议 40 的倍数」），非法值不写入
- [ ] 自动保存：停止操作约 1.5 秒后写入，Toast「已保存，鼠标 OLED 已切换」；切档即时生效
- [ ] 底部常驻当前档位指示：`当前档位：第 3 档 · 1600 DPI`（硬件切档时跳动更新）

### Phase 4：按键映射页面（约 0.5 天）

**目标**：选择按钮 → 设置功能 → 自动保存写入，防止误清空整表。

> ⚠️ **前置（功能闭环缺口 ①）**：0x08 功能码表目前仅收录已验证的 12 个
> （标准/左/右/中/前进/后退/滚轮×4/DPI循环/宏，见 `HID协议逆向报告.md` 3.5 节）。
> 官方 Wiki 的**媒体键、快捷键、浏览器、系统、双击、火枪键、简易瞄准、LED循环、DPI固定值/±、
> Profile循环/±、按键关闭**等功能码**均未逆向**。开工本页前须先用 USBPcap 抓官方软件改这些键，
> 补齐 0x08 功能码表（并行工作流，不影响本页框架搭建）。**未确认编码一律不进 UI 下拉。**

**功能**
- [ ] `ButtonViewModel`：18 按钮名称数组、功能下拉（已确认 12 码 + 分组）、`LoadConfig`/`SaveConfig`（防抖）
- [ ] 写入：克隆本地全表 → 修改目标 entry → 整表发送（0x08 无增量，见 AGENTSK 6 节）
- [ ] 宏绑定：选择功能「宏」→ 二级选择宏 ID 与播放方式

**UX/UI**
- [ ] 左：**鼠标外形示意图**（10 个可编程点，悬停高亮 + tooltip 按钮名，点击选中）——比下拉列表直观得多
- [ ] 右：选中按钮的「功能分组列表」（鼠标操作 / DPI / 配置切换 / 宏 / 快捷键 / 多媒体 / 浏览器 / 系统 / 关闭），官方 Wiki 同款分组
- [ ] 当前 3 字节原始值显示区（小字、Consolas、可展开，面向进阶用户，默认折叠）
- [ ] **防呆**：自动保存前弹确认「将更新整张按键映射表，其他未修改按键保持当前配置，继续？」；保存后 Toast
- [ ] 说明文案：「未识别的功能暂不提供（等待协议补齐）」置灰显示缺口项，避免误导

### Phase 5：宏管理页面（约 1 天，UX 最重）

**目标**：录制动作序列 → 选绑按钮 → 写入设备。

> ⚠️ **功能闭环缺口 ②**：Phase 2 已实现键盘录制/延迟编辑/选中删除/清空/3 种播放方式/循环次数，
> 尚缺：**鼠标动作（左/右/中键）插入、实时录制（Hook）、宏命名管理（替代固定 6 槽位）、指令上移/下移、导入/导出**。
> 下方「缺口②闭环策略」给出每项的可落地方案；**均为纯软件层工作，不依赖协议逆向**，可随 Phase 5 一并完成，不阻塞发布。
> **延迟语义注意**（AGENTSK 6 节）：UI 显示的「设备实际延迟」与 PC 输入值需明确区分——`PcInputToActualMs` 已封装换算，
> 本页延迟输入一律按「设备实际生效延迟」标注，避免「填 1000 实际 50ms」的困惑。

**缺口②闭环策略（逐项落地）**
- **鼠标动作插入**：`AddMouseAction(MouseButton btn, bool isDown)` 在动作序列中追加「鼠标左/右/中键 · 按下/释放」。
  编码复用 `Models.cs` 现有键码表（左键=0x01 / 右键=0x02 / 中键=0x04，具体以 `HID协议逆向报告.md` 3.5 节为准）；
  若报告未列出鼠标动作码，**先查 `reference/` 脚本是否已验证，未验证则置灰「插入鼠标动作」并标注「等待协议补齐」**，绝不盲发。
- **实时录制（Hook）**：用 `System.Windows.Forms` 的 `KeyboardHook` + `MouseHook`（或 `globalmousekeyhook` 等价 P/Invoke，
  **不引入额外 NuGet，沿用框架自带能力**）在录制态捕获按键/键鼠事件，按时间顺序回填序列并自动在事件间插入合理延迟。
  录制态全局拦截 **Esc** 结束；录制期间禁用其他导航以免丢失焦点。Hook 仅在「录制中」激活，结束即卸载，避免常驻拖慢。
- **宏命名管理（替代固定 6 槽位）**：`MacroConfig` 增加 `Name` 字段；本地用 `ObservableCollection<MacroConfig>` 管理，
  设备侧仍按宏 **ID（0~5）** 写入（维持 6 槽位硬件上限），命名仅为上位机可读性 → 写入时把「命名宏」映射到空闲 ID，
  并在 `ProfileStore` 持久化「名称↔ID」映射，跨 Profile 切换不丢名。
- **指令上移/下移**：`MoveUp(index)` / `MoveDown(index)` 交换序列相邻元素；列表选中行高亮，按钮在选中行禁用态自洽。
- **导入/导出**：导出为 JSON（结构 `{ name, actions:[{type,code,down,delayMs}], playMode, loopCount }`），
  导入时校验 `delayMs` 经 `PcInputToActualMs` 还原回设备值再写；导入文件非法/版本不符 → 提示「文件无法识别，请检查是否本软件导出」，
  **不静默写入**，避免损坏宏表。
- **明细补齐到功能清单**：上面 5 项已拆进下方 `功能` 与 `UX/UI` 的 checkbox，逐项验收。

**功能**
- [ ] `MacroViewModel`：宏列表（命名/新建/删除）、动作序列（键盘/鼠标/延时/按下/释放）、三种播放方式
- [ ] `AddKeyAction` / `AddDelayAction` / `AddMouseAction` / `DeleteAction` / `MoveUp` / `MoveDown` / `Clear`
- [ ] 实时录制：`StartRecord`（Hook 激活，Esc 结束，自动回填 + 插入间隔延迟）/ `StopRecord`
- [ ] 宏命名：`RenameMacro` / `DuplicateMacro`（替代固定 6 槽位，写入时映射到设备宏 ID 0~5）
- [ ] `WriteMacro` → `MacroConfig.BuildMacroChunks` 逐块发送（0.2s 间隔）
- [ ] 导入/导出：`ExportMacro(Json)` / `ImportMacro(Json)`，导入前校验结构 + 延迟语义还原，非法文件拒绝写入

**UX/UI**
- [ ] 左侧宏列表（可重命名，右键或「⋯」菜单：重命名/复制/删除/导入/导出）
- [ ] 右侧编辑区：动作表格（按键 | 动作 | 延迟ms 三列），选中行支持上移/下移/删除
- [ ] 鼠标动作插入入口（左/右/中键 · 按下/释放）；若鼠标动作码未验证则置灰 + 「等待协议补齐」
- [ ] 「录制」按钮：点击后按钮变红并呼吸闪烁 + 文案「录制中…按 Esc 停止」，Hook 捕获键鼠并自动回填 + 插入间隔延迟
- [ ] 延迟列直接编辑数字；「设备实际延迟」提示文字常驻表格上方
- [ ] 播放方式三选一卡片 + 循环次数输入（仅在「循环次数」方式下启用）
- [ ] 原始字节预览区（可折叠，进阶）
- [ ] 未绑定警告：自动保存宏前若该宏未被任何按键引用，提示「此宏尚未绑定到按键，可在按键页使用」
- [ ] 导入/导出：列表「⋯」菜单提供「导出此宏 / 导入宏」，导入非法文件时弹「文件无法识别，请检查是否本软件导出」且不写入

### Phase 6：灯光 / 回报率 / 电池 / 电源页迁移（约 0.5 天）

**目标**：把 Phase 2 已打通的 0x05/0x06/电池 Input Report 迁入原生 WPF 界面风格。

**功能**
- [ ] `LightViewModel` / `RateViewModel` / `BatteryViewModel`：直接复用 `Models.cs` 的 `LightConfig`/`RateConfig` 与电池解析
- [ ] 沿用发送时序：0x0C 唤醒、数据设备 0x0A 前置

**UX/UI**
- [ ] 灯光页：5 种灯效模式（大图卡片或下拉）+ 速度滑块 + 「移动时关闭灯光」开关；灯效图标化（呼吸/常亮/循环/霓虹各自图标）
- [ ] 回报率页：**圆形仪表盘式选择器**（官方同款，四象限 125/250/500/1000 Hz，中心显示模式名「游戏模式」）
- [ ] 去抖滑块：1-25ms，实时显示数值，旁注「数值越小响应越快，误触概率越高」
- [ ] 电池页：**电池图标式进度条**（红→绿渐变），实时刷新（监听 Input Report 每 ~2s），显示百分比
- [ ] ⚠️ **电源管理**：独立分区，顶部红色警示条「固件对自定义电源命令反应不稳定，有断联风险，建议使用官方软件操作」；默认提供「关闭电源设置」的推荐选项；若允许修改，先检测数据设备已打开并二次确认
- [ ] 灯光速度与去抖修改为「即时预览 + 自动保存」两段式：预览实时更新 UI，停止操作约 1.5 秒后写入，避免频繁写设备

### Phase 7：配置管理（Profile）页面（约 0.5 天）

**目标**：多套配置方案的管理与切换。

> ⚠️ **功能闭环缺口 ③**：Profile 为纯软件层（设备无槽位），切换 = 全量写
> 0x0C→0x04→0x05→0x06→0x08→0x09×3（AGENTSK 7 节）。本地 JSON 持久化每套全套值。
> 下方「缺口③闭环策略」细化**回滚与原子性**——这是本页唯一的技术不确定性点。

**缺口③闭环策略（回滚 / 原子性）**
- **写入前快照（关键）**：切换开始时，**先从 `HidService` 抓取设备当前 9 模块全套值**作为 `preSnapshot`，
  同时把目标 Profile 全套值记为 `target`。进度每推进一步（1/8…8/8）都更新进度条。
- **失败判定与回滚**：任一步 `SendSequence` 抛异常 / 返回失败 / 超时 → **立即中止后续步骤**，
  用 `preSnapshot` 重新全量写回设备，恢复到切换前状态；回滚本身也走进度条「正在恢复…」。
  回滚若再次失败 → 顶部红色错误条「配置恢复失败，设备可能处于不一致状态，建议重连后手动校对」，**不静默吞错**。
- **本地状态一致性**：无论切换成功/失败，本地 `activeProfileId` 只在**全部 8 步成功**后才更新；
  失败则保持原 Profile 高亮，避免「UI 显示 A、设备实际 B」的分裂。
- **鼠标切档/宏绑定跟随**：回滚后不触发「未保存」角标（视为设备已对齐快照）；`OnDpiLevelChanged` 等监听照常同步高亮。
- **电源命令风险叠加**（见风险表电源项）：切换若含 0x05 电源字节，先确认数据设备 0x0A 已开，否则该步单独二次确认，
  失败优先回滚而非继续，防止断联扩散。

**功能**
- [ ] `ProfileViewModel`：配置列表（新建/删除/重命名/上移/下移）、每套保存 9 模块全套值
- [ ] 「保存当前设备状态为新 Profile」入口（一次抓取当前全部配置快照）
- [ ] 切换前抓取 `preSnapshot`；手动切换：选中 → 全量写入（0x0C→0x04→0x05→0x06→0x08→0x09×3，0.2s 间隔）；写入进度条（8 条报告逐条推进）
- [ ] 失败回滚：任一步失败立即中止并用 `preSnapshot` 全量写回，回滚也走进度；回滚失败红色错误条不静默
- [ ] `activeProfileId` 仅在 8 步全部成功后更新，避免 UI/设备状态分裂
- [ ] 默认内置「日常」「游戏」两套（对齐官方）

**UX/UI**
- [ ] 左侧配置列表（选中高亮），右侧当前 Profile 摘要卡片（DPI/灯光/回报率/按键数概览）
- [ ] 新建向导：命名 → 「从当前设备复制」或「从默认值开始」
- [ ] 切换时进度显示「正在应用第 3/8 步…」；失败中断并提示「正在恢复原配置…」，成功才标记当前 Profile
- [ ] 回滚失败红色错误条「配置恢复失败，设备可能不一致，建议重连校对」，不静默
- [ ] 删除/覆盖二次确认

### Phase 8：体验打磨 + 发布（约 1 天）

**目标**：让程序「像一个正经的 C 端产品」。

**功能**
- [ ] 程序图标（鼠标造型，替换默认 WPF 图标）、`App.config` PerMonitorV2 DPI 感知
- [ ] **其他设置页（`OtherSettingsPage`，蓝本见原型文档第九节）**：电量展示（复用 `BatteryViewModel`）/ 产品外观黑·白选择（本地持久化，影响全站鼠标示意图配色）/ 恢复出厂（红色危险按钮 + 二次确认，软件默认值覆写全套报告，无硬件重置命令）/ 官方驱动下载入口 / 关于（版本号 + 免责声明）
- [ ] 关于对话框：版本号、产品名、免责声明（非官方软件）
- [ ] 发布脚本：`dotnet publish -c Release -r win-x64 --self-contained` 单文件（对齐 README 构建节）
- [ ] 启动自检：能否连设备 / 官方驱动是否运行 / 配置存储是否可写

**UX/UI**
- [ ] **其他设置页五张卡片**：电池 / 产品外观 / 维护（恢复出厂）/ 官方驱动 / 关于，信息分层清晰（对齐原型文档 9.1~9.6）
- [ ] 恢复出厂二次确认弹窗 + 提示「请先关闭官方驱动」；与侧边栏「恢复出厂」入口共用同一危险操作保护
- [ ] 全站文案走查：同一操作同一动词（统一用「保存」/「恢复出厂」），错误提示给「怎么办」不给「为什么」报错码
- [ ] 主题：浅色/深色切换（`App.ApplyTheme` 切换 `Themes/` 资源字典），默认跟随系统
- [ ] 窗口记忆：记住上次窗口位置/大小/主题/最后停留页
- [ ] 首次启动：无复杂引导，直接进 DPI 页；「帮助」菜单放 FAQ（含「为什么检测不到鼠标」排查清单）
- [ ] 接入崩溃日志上报入口（crash.log 存在时提示「上次异常退出，发送日志以便改进」）

---

## 五、已知风险 & 应对（沿用 + 补充 UX 视角）

| 风险 | 应对（协议层见 reference，此处列 UX 侧） |
|---|---|
| 第三方 UI 库与现有 `HidNative.cs` P/Invoke 不兼容 | 本项目**不使用任何第三方 UI 库**（纯原生 WPF）；HidNative 是纯静态类，与原生控件无冲突 |
| 断连后 ViewModel 状态残留 | 断开时重置 `_isConnected`；保留未保存编辑；UI 禁止写入 |
| 宏写入时 Mouse.exe 未退出导致断连 | 连接前检测进程 → 顶部黄色警告条 + 阻断发送并给「关闭官方软件」引导 |
| 用户改错配置 → 设备异常 | 危险操作二次确认；未保存编辑可放弃；「恢复出厂」在其他设置一键回默认 |
| **电源管理（0x05 byte5/9）固件行为不稳定**：曾 2.4G 断联（AGENTSK 2.3c） | 电源页红色警示条；默认降级「用官方软件操作」；修改需确认数据设备已开 + 二次确认 |
| **媒体/快捷键等功能码未逆向**：盲发写坏按键表（2.3b 教训） | 缺口功能置灰 + 「等待协议补齐」文案；未确认编码不进 UI |
| **Profile 切换无设备命令**：全量写 8 条 | 进度条逐条反馈；失败可回滚到上一个 Profile |
| **宏延迟语义易误解**（UI 值 ≠ 设备实际） | 延迟列按「设备实际生效」标注，`PcInputToActualMs` 换算已封装 |
| 官方 Mouse.exe 抢占导致命令失效 | 全站统一「连接前置检查」：发送前若检测到 Mouse.exe 则拦截并引导 |

---

## 六、验收标准

**连接与全局**
- [ ] 启动自动连接；未连接时全部页面显示引导空态，禁止写入
- [ ] 检测到官方 Mouse.exe 运行 → 黄色警告条，发送被拦截并给引导
- [ ] 断连/重连状态条与页面禁用即时切换；已修改未保存内容不丢失，重连后自动保存

**各页**
- [ ] DPI 页：改值→自动保存→OLED 生效；按鼠标 DPI 键→UI 高亮同步；非法输入被拦截
- [ ] 按键页：鼠标示意图选键→分组功能→自动保存→实机生效；待保存修改有明确标记；整表覆写前有确认
- [ ] 宏页：录制→编辑→绑定→实机触发；命名/上移下移/导入导出可用；延迟按设备实际语义标注
- [ ] 灯光页：5 模式+速度+移动关灯生效；电源分区带红色风险提示
- [ ] 回报率页：仪表盘 4 档选择生效；去抖滑块实时显示
- [ ] 电池页：电量图标实时刷新（每 ~2s）
- [ ] Profile 页：新建/切换多套配置，切换进度可见，全部生效；失败可回滚

**体验**
- [ ] 全站文案统一、错误提示给「怎么办」；深色模式可用
- [ ] 窗口状态记忆；单文件发布包脱离 .NET 运行；`crash.log` 存在时提示上报

---

## 七、备注

- 协议事实（校验和、时序、功能码状态）一律以 `reference/HID协议逆向报告.md` 与 `reference/AGENTSK_knowledge.md` 为准，本文档不复制协议字节细节。
- **UI 规格蓝本**：各页视觉布局、组件、交互与文案以 `M618XSD客户端原型描述.md` 为准（其承载 9 个页面完整原型，含其他设置页、连接等待页、侧边栏导航、产品外观等本计划未细画的细节）。本计划定「做什么/验收」，原型定「长什么样/怎么用」，冲突以协议事实与用户体验优先裁决。
- UI 缺口项（媒体键/快捷键/Profile 绑定等）依赖 0x08 功能码逆向，属并行工作流，不阻塞本框架搭建。
- 每完成一个 Phase，同步在 `reference/README.md` 与 `AGENTS.md` 阶段表中更新状态。
